/* eslint-disable */

export type CarouselConfig = {
  images: any[];
  visibleCount: number;
  current: number;
  interval: number;
  outbound: boolean;
  onSlideChange: Function;
};

/* eslint-enable */
